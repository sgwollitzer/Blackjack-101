import React from 'react';

const Buttons = () => {
  return (
    <div className="buttons">
      <button>New Game</button>
      <button>Hit</button>
      <button>Stand</button>
      <button>Split</button>
    </div>
  );
};

export default Buttons;